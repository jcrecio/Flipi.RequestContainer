# Flipi.RequestContainer
Container that stores all the destinations that need to be fetched to get the flights periodically

<pre>
In order to run the app you have to:


With docker:

- docker-compose build
- docker-compose up
</pre>
